# 天鹅输入法

[English](./README.en.md)

天鹅输入法是基于 [Fcitx5 for Android](https://github.com/fcitx5-android/fcitx5-android) 深度定制的一版 Android 输入法。  
当前主程序应用名为 `天鹅输入法`，主程序包名为 `org.fxboomk.fcitx5.android`。

这个分支重点强化了两条能力：

- 剪贴板能力：把本机剪贴板、其他设备剪贴板、图片/文件剪贴板做成了更完整的一套工作流。
- 预测与扩展能力：保留 Fcitx5 的多输入方案体系，同时增强了 RIME 等插件扩展场景。

## 最近更新

这里提炼最近 20 个提交后，当前这一轮更新主要集中在下面几个方向：

- 键盘布局编辑、预览与管理全面重做：
  - 新增独立的“键盘布局管理”页，集中列出所有布局：可设为默认、上移排序、折叠收纳，并由此进入逐键编辑/重置、二维码导入分享，以及布局的增删改；排序与折叠状态会被持久化保存。
  - 布局编辑器精简为“内容编辑 + 实时预览”，新增“切换布局 / 管理布局”溢出菜单，并支持从管理页深链直达对应布局。
  - 键盘高度支持三级覆盖（布局档案 < 基础布局 < 子模式），空格键预览会显示大写字母与当前布局名。
  - 打开布局编辑器时改为直接读取已部署的 Rime schema_list 解析可用方案，不再强行切换到目标输入法，你正在使用的输入法不会被动更改；插件未加载时自动隐藏 Rime 布局。
  - 布局预览重做为固定高度容器，背景严格贴合一个键盘高度而不再纵向溢出；预览 Rime 布局时使用 Rime 引擎自身的空格键标签、图标与身份，不再错误继承当前输入法（例如内置双拼的“双”）。
- 候选与手势更快、更不易误触：
  - 候选支持上滑选字、下滑重置词频，并在候选区之外提供明确的取消区域；展开候选窗采用更紧凑的自适应网格。
  - 拆字与重置词频手势改为“先长按候选、再上/下滑”触发：普通上下滑不再误触发这两个动作，从而把上下滑动让给展开候选窗的滚动。
  - 长按候选新增“提交首字 / 末字”菜单项（基于 BreakIterator 提取候选串的首、末字符）。
- 键盘按键行为更可定制：
  - 新增可定制的大写键切换行为：持久大写锁定改用 Shift 语义而非物理 CapsLock，避免 Rime 把 CapsLock 当作模式切换而误提交；大写锁定状态在键盘重建、切换布局后也能保留。
  - MacroKey 新增按物理滑动方向（上 / 下）触发的宏动作，优先级高于按显示方向绑定的 alt-label 滑动；旧版单一 swipe 字段作为兼容回退，编辑器首次编辑时自动迁移，按键编辑器也把“滑动事件”拆成“上滑事件 / 下滑事件”。
  - 符号按键标签改由 KeyDefPreset 中的映射表统一转成 ASCII 等价字符（如“。”→“.”）再送入引擎，逻辑更清晰、可测试；提交非 ASCII 自定义符号标签前会先顶字。
- 设置、方案管理与本地化细节修正：
  - 打开设置页不再自动抢占焦点弹出键盘：搜索框改为点击占位符后才实例化并聚焦，离开页面时重置查询，定位到搜索结果时高亮目标项。
  - Rime 方案管理改由 Android 菜单路由到独立的方案管理实现。
  - 修正语言区域解析：从 languageWithCountry 正确推导裸语言码，并把 zh_*_#Hans/Hant 回退到 zh_CN / TW。

## 项目特点

### 1. 更强的剪贴板体系

- 主程序剪贴板支持多分类视图：
  - 本机
  - 其他设备
  - 图片文件
  - 全部
- 图片类条目支持缩略图预览。
- URL 条目支持右键菜单直接“打开链接”。
- 图片条目支持右键菜单“查看图片”，调用系统默认程序打开。
- 图片、文件类剪贴板条目会尽量以 URI 形式保留，方便后续发送文件而不是只发送文本。
- 长文本（超过 10 个字符）类型的剪贴板条目支持分词分块展示，便于浏览和二次使用。
- 剪贴板历史记录上限可分别按分类设置。

### 2. 剪贴板同步插件

项目已集成 `clipboard-sync` 插件构建链路，可与主程序一起构建和安装。

当前剪贴板同步插件支持：

- [`OneClip`](https://oneclip.cloud/)
- `ClipCascade`
- [`SyncClipboard`](https://github.com/Jeric-X/SyncClipboard)

插件能力包括：

- 自动拉取桌面端剪贴板记录到手机。
- 支持文本、图片、文件等多种剪贴板内容。
- 支持手动上传剪贴板条目到服务端，方便按需同步。
- 支持后台保活、断线重连、前台服务常驻、控制磁贴开关。
- OneClip 支持带访问令牌的拉取、推送、图片和文件下载。
- 文件同步支持保存到指定目录，并尽量保留原始文件名中的中文等 Unicode 字符。
- 支持同步过滤：
  - 文本长度过滤
  - 文件后缀过滤
  - 最大文件大小过滤
- 支持测试连接、测试推送、系统剪贴板授权引导等设置项。
- 近期又补了一轮网络策略限制和 UI 细节修正，使同步状态、权限引导和异常场景反馈更清晰。

如果你的使用场景是“电脑复制，手机输入”或“手机复制，电脑接收”，这一版已经把它当成核心能力来打磨。

### 3. 预测与输入方案扩展

- 保留 Fcitx5 原有的多语言输入方案框架。
- 中文输入继续支持拼音、双拼、五笔、仓颉、码表等能力。
- 保留并可加载 RIME 插件，适合自定义方案、词库和高级配置。
- 保留 libime / 中文插件链路中的候选预测、联想与语言模型能力。
- 键盘拉起即用：fcitx 引擎保温运行，切回输入法或退出设置页时不再重复 Rime 初始化与数据同步。
- 输入热路径经过多轮性能优化：减少每次按键的 Binder 往返与冗余候选渲染，并提升 fcitx 线程调度优先级。
- 增强了 [`librime`（中州韵插件）](https://github.com/boomker/librime)的预测能力，可以基于用户输入历史持续学习，并支持预测数据备份。
- 新增和扩展了 AI 预测/问答相关能力，可接入不同模型提供方，并对流式响应、失败回退、请求节流做了专门处理。
- 当前 AI 提供方列表已包含 Moonshot 等选项。
- AI 候选支持展开式候选窗口，可直接浏览更多预测结果。
- 横向候选栏支持独立调整项目间距与高亮样式，分页时会正确刷新候选内容。
- 输入法重启后会保留手动选择的键盘布局，减少布局被意外切回默认值的情况。
- 候选交互增强：支持上滑选字、下滑重置词频，长按候选可提交候选串的首字或末字；拆字、重置词频等动作改为长按候选后再上下滑触发，避免普通滑动误触，也把上下滑动让给展开候选窗滚动。
- 键盘层面的可调能力持续增强：MacroKey（含按物理方向的上 / 下滑动宏）、可定制的大写键切换、Shift 行为开关、popup 手势滑动选择候选项等。

### 4. 键盘布局与 popup 配置分享

- 新增独立的“键盘布局管理”页：集中管理全部布局（设为默认、上移排序、折叠、增删改、二维码导入分享），布局编辑器则专注内容编辑与实时预览，并可从管理页深链直达；排序与折叠状态会被持久化保存。
- 文本键盘布局和 popup 预设支持二维码分享。
- 支持摄像头扫码导入，也支持从文件导入分享数据。
- 分享二维码生成前可直接预览图像内容，方便确认后再发送。
- 文本键盘布局 JSON 支持直接配置按键颜色，便于分享完整的布局视觉方案。
- 文本键盘布局 JSON 支持为不同布局分别设置竖屏和横屏键盘高度。
- Rime 输入方案（子模式）支持单独设置键盘高度，切换方案后立即生效，布局预览同步遵循。
- 键盘高度支持三级覆盖：布局档案 < 基础布局 < 子模式，逐层生效。
- 打开布局编辑器不再切换你正在使用的输入法（改为读取已部署的 Rime schema_list 解析可用方案）；预览 Rime 布局会使用 Rime 引擎自身的身份、空格键标签与图标，预览背景也严格贴合一个键盘高度。
- 键盘布局编辑器支持上移整行，自定义按键的备用字符会保留原始文本。
- MacroKey 支持按物理滑动方向（上 / 下）配置宏动作，按键编辑器把“滑动事件”拆分为上滑、下滑两项，并自动迁移旧版配置。
- 工具栏和键盘按钮支持配置切换数字键盘的操作。
- 自定义键盘按键可配置大写提示，编辑器会用独立边框标记已修改的按键。
- popup 预设编辑支持恢复默认候选内容，并可继续通过二维码导入导出。

### 5. 工具栏与界面可定制性

- 工具栏按钮支持图标字体和 drawable 两种图标来源，方便统一风格。
- 工具栏按钮图标支持通过 iconfont 字符点位自定义，适合使用统一的图标字体风格。
- 工具栏配置预览支持自定义按钮图标和字体大小，图标会根据主题颜色显示。
- 主设置页支持搜索，可从搜索结果跳转到跨页面设置项并自动定位、高亮；打开设置页时搜索框不再自动抢占焦点弹出键盘，点击占位符后才激活。
- 布局、按键、字体、popup 等编辑器使用带状态提示的保存图标。
- 输入栏内置更语义化的隐藏键盘图标，界面辨识度更高。
- 状态区在 Rime 引擎下提供默认页与 Rime 页双页签，点击或滑动切页，当前页签高亮显示。
- 输入法选择器、键盘调节浮层、浮动键盘状态保持等界面细节持续优化，主题切换时布局刷新更稳定。

### 6. 应用内更新与版本获取

- 关于页已内置“检查更新”入口，可直接对比 GitHub Releases 上的最新正式版本。
- 检测到新版本后，会先下载与当前应用包名、设备 ABI 匹配的 APK，再由用户手动点击“安装更新”执行安装。
- 本地构建和 CI 产物的版本段已改成“日期 + 提交短哈希”，更方便定位问题来源。
- 下载应用或插件更新时会显示确定进度，并支持取消当前下载。

### 7. 主题多选切换

- 亮色模式和暗色模式主题支持勾选多个主题。
- 点击明暗切换按钮时，会在当前模式的多个主题间轮询切换。
- 每个模式最多可添加多个主题，方便快速切换不同视觉风格。
- 主题编辑支持更明确的保存、分享和导入操作，主题预览会跟随当前键盘行为更新。
- 主题导入兼容其他 fork 主题 JSON 中的未知字段，第三方主题也能顺利导入。

### 8. 保持 Fcitx5 的插件化结构

- 主程序继续通过插件机制加载额外输入法引擎或功能插件。
- 当前仓库除主程序外，还包含 RIME、Anthy、UniKey、Thai、Hangul、Sayura、Jyutping 等插件/组件。
- 对外部插件安装与联动场景保持兼容。

## 支持的输入方案

- English
- 中文
  - 拼音 / 双拼 / 五笔 / 仓颉 / 自定义码表
  - 注音
  - 粤拼
- Vietnamese
- Japanese
- Korean
- Sinhala
- Thai
- RIME 自定义输入方案

## 下载

- GitHub Releases:
  [https://github.com/boomker/fcitx5-android/releases](https://github.com/boomker/fcitx5-android/releases)

如果你只想体验当前维护版本，优先使用 Releases 页面中的正式包。

如果已经安装主程序，也可以在应用内通过“关于 -> 检查更新”直接下载并安装最新正式包。

## 构建

### 依赖

- Android SDK Platform / Build-Tools 36
- Android NDK 28
- CMake 3.31.6
- `extra-cmake-modules`
- `gettext`

当前应用最低支持 Android 7.0（API 24）。

### 初始化仓库

```sh
git clone git@github.com:boomker/fcitx5-android.git
cd fcitx5-android
git submodule update --init --recursive
```

### 本地构建

```sh
./gradlew assembleDebug
```

如果要单独构建剪贴板同步插件：

```sh
./gradlew :plugin:clipboard-sync:assembleRelease
```

## 适合谁

- 希望在 Android 上使用 Fcitx5 体系输入法的人。
- 需要 RIME、自定义码表、多输入方案切换的人。
- 需要手机与桌面端双向同步剪贴板的人。
- 需要把图片、文件、链接等内容也纳入输入法剪贴板工作流的人。

## 致谢

- 原始项目：
  [fcitx5-android/fcitx5-android](https://github.com/fcitx5-android/fcitx5-android)
- 分支与功能增强参考：
  [fxliang/fcitx5-android](https://github.com/fxliang/fcitx5-android)
- Fcitx5 / libime / fcitx5-chinese-addons / RIME 等上游项目贡献者
